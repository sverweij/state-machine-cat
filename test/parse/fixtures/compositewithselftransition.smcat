outer {
    inner.one,
    inner.two {
        regular => regular: regular self transition;
    };
    inner.one => inner.one: regular self transition;
    inner.two => inner.two: self transition of composite state that has a parent;
};
outer => outer: self transition of composite state that has no parent;